function drawArena() {
	ctx0.drawImage(bgArenaImg, 0, (canHeight - tHeight) * 0.5, tWidth, tHeight);
}

function drawBackground() {
	ctx0.drawImage(bgFieldImg, 0, (canHeight - tHeight) * 0.5, tWidth, tHeight);
}